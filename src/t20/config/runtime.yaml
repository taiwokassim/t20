# config/runtime.yaml
default_agent: Orchestrator
max_concurrent_agents: 3
logging_level: INFO
api_endpoints:
  search: "https://api.example.com/search"
  summarize: "https://api.example.com/summarize"
